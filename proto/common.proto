syntax = "proto3";

package common;

option go_package = "proto/commonpb";

enum Type {
  UNKNOWN = 0;
  UPDATE_RATE = 1; // change telemetry interval
  PING = 2;        // health check
  SHUTDOWN = 3;    // stop simulator
}

message Command {
    Type type = 1;
    uint32 value = 2;
}
